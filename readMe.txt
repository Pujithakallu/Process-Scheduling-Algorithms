To run the code:
cd to the 'source-code' folder in the terminal and run the below commands:
gcc -o main *.c
./main

To save the output in the file, execute the binary file as
./main > output.txt